---
layout: default
---
{% unless site.data.resume %}
  <div class="post">
    <header class="post-header">
      <h1 class="post-title">{{ page.title }}</h1>
      {% if page.cv_pdf %}
        {% if page.cv_pdf contains '://' %}
          {% assign cv_pdf_url = page.cv_pdf %}
        {% else %}
          {% assign cv_pdf_url = page.cv_pdf | prepend: 'assets/pdf/' | relative_url %}
        {% endif %}
        <p class="mt-1 mb-0">
          <a href="{{ cv_pdf_url }}" target="_blank" rel="noopener noreferrer" style="font-size: 1.25rem;">
            <i class="fa-solid fa-file-pdf"></i> Download CV
          </a>
        </p>
      {% endif %}
      {% if page.description %}
        <p class="post-description">{{ page.description }}</p>
      {% endif %}
    </header>

    {% if page.cv_pdf %}
      <div class="cv-pdf-viewer mt-3 mb-4">
        <iframe src="{{ cv_pdf_url }}" width="100%" height="900" style="border: 1px solid #ddd; border-radius: 4px;"></iframe>
      </div>
    {% endif %}

    <article>
      <div class="cv">
        {% for entry in site.data.cv %}
          <a class="anchor" id="{{ entry.title }}"></a>
          <div class="card mt-3 p-3">
            <h3 class="card-title font-weight-medium">{{ entry.title }}</h3>
            <div>
              {% if entry.type == 'list' %}
                {% include cv/list.liquid %}
              {% elsif entry.type == 'map' %}
                {% include cv/map.liquid %}
              {% elsif entry.type == 'nested_list' %}
                {% include cv/nested_list.liquid %}
              {% elsif entry.type == 'time_table' %}
                {% include cv/time_table.liquid %}
              {% elsif entry.type == 'list_groups' %}
                {% include cv/list_groups.liquid %}
              {% else %}
                {{ entry.contents }}
              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  </div>
{% else %}
  <div class="post">
    <header class="post-header">
      <h1 class="post-title">{{ page.title }}</h1>
      {% if page.cv_pdf %}
        {% if page.cv_pdf contains '://' %}
          {% assign cv_pdf_url = page.cv_pdf %}
        {% else %}
          {% assign cv_pdf_url = page.cv_pdf | prepend: 'assets/pdf/' | relative_url %}
        {% endif %}
        <p class="mt-1 mb-0">
          <a href="{{ cv_pdf_url }}" target="_blank" rel="noopener noreferrer" style="font-size: 1.25rem;">
            <i class="fa-solid fa-file-pdf"></i> Download CV
          </a>
        </p>
      {% endif %}
      {% if page.description %}
        <p class="post-description">{{ page.description }}</p>
      {% endif %}
    </header>

    {% if page.cv_pdf %}
      <div class="cv-pdf-viewer mt-3 mb-4">
        <iframe src="{{ cv_pdf_url }}" width="100%" height="900" style="border: 1px solid #ddd; border-radius: 4px;"></iframe>
      </div>
    {% endif %}

    <article>
      <div class="cv">
        {% for data in site.data.resume %}
          {% if site.jsonresume and site.jsonresume.size > 0 %}
            {% unless site.jsonresume contains data[0] %}
              {% continue %}
            {% endunless %}
          {% endif %}
          {% if data[0] == 'meta' or data[1].size == 0 %} {% continue %} {% endif %}
          <a class="anchor" id="{{ data[0] }}"></a>
          <div class="card mt-3 p-3">
            {% assign section_title = data[0] | capitalize %}
            {% if data[0] == 'work' %}{% assign section_title = 'Research Experience' %}
            {% elsif data[0] == 'publications' %}{% assign section_title = 'Research' %}
            {% elsif data[0] == 'projects' %}{% assign section_title = 'Software Development' %}
            {% elsif data[0] == 'teaching' %}{% assign section_title = 'Teaching Experience' %}
            {% elsif data[0] == 'volunteer' %}{% assign section_title = 'Professional Service' %}
            {% elsif data[0] == 'awards' %}{% assign section_title = 'Fellowships & Grants' %}
            {% elsif data[0] == 'certificates' %}{% assign section_title = 'Other Relevant Training' %}
            {% elsif data[0] == 'referee' %}{% assign section_title = 'Referee' %}
            {% endif %}
            <h3 class="card-title font-weight-medium">{{ section_title }}</h3>
            <div>
              {% case data[0] %}
                {% when 'basics' %}
                  {% include resume/basics.liquid %}
                {% when 'education' %}
                  {% include resume/education.liquid %}
                {% when 'work' %}
                  {% include resume/work.liquid %}
                {% when 'teaching' %}
                  {% include resume/teaching.liquid %}
                {% when 'affiliations' %}
                  {% include resume/affiliations.liquid %}
                {% when 'presentations' %}
                  {% include resume/presentations.liquid %}
                {% when 'referee' %}
                  {% include resume/referee.liquid %}
                {% when 'volunteer' %}
                  {% include resume/volunteer.liquid %}
                {% when 'projects' %}
                  {% include resume/projects.liquid %}
                {% when 'awards' %}
                  {% include resume/awards.liquid %}
                {% when 'skills' %}
                  {% include resume/skills.liquid %}
                {% when 'publications' %}
                  {% include resume/publications.liquid %}
                {% when 'languages' %}
                  {% include resume/languages.liquid %}
                {% when 'interests' %}
                  {% include resume/interests.liquid %}
                {% when 'certificates' %}
                  {% include resume/certificates.liquid %}
                {% when 'references' %}
                  {% include resume/references.liquid %}
                {% else %}

              {% endcase %}
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  </div>
{% endunless %}
